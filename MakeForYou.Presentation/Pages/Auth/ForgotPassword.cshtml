@page
@model MakeForYou.Presentation.Pages.Auth.ForgotPasswordModel
@{
    ViewData["Title"] = "Quên Mật Khẩu";
}

<div class="container d-flex justify-content-center py-5">
    <div class="card shadow-sm w-100" style="max-width: 440px;">
        <div class="card-body p-4">

            <h3 class="card-title mb-1 fw-bold">Quên Mật Khẩu</h3>
            <p class="text-muted mb-4">Nhập email của bạn và chúng tôi sẽ gửi cho bạn liên kết đặt lại mật khẩu.</p>

            @if (!string.IsNullOrEmpty(Model.SuccessMessage))
            {
                <div class="alert alert-success" role="alert">
                    <i class="bi bi-envelope-check me-2"></i>@Model.SuccessMessage
                </div>
            }

            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="alert alert-danger" role="alert">
                    <i class="bi bi-exclamation-circle me-2"></i>@Model.ErrorMessage
                </div>
            }

            @if (string.IsNullOrEmpty(Model.SuccessMessage))
            {
                <form method="post" novalidate>
                    @Html.AntiForgeryToken()

                    <div class="mb-4">
                        <label asp-for="Input.Email" class="form-label">Email <span class="text-danger">*</span></label>
                        <input asp-for="Input.Email" class="form-control" placeholder="example@email.com" autofocus />
                        <span asp-validation-for="Input.Email" class="text-danger small"></span>
                    </div>

                    <button type="submit" class="btn btn-primary w-100 py-2 fw-semibold">
                        Gửi Liên Kết Đặt Lại
                    </button>
                </form>
            }

            <hr class="my-3" />
            <p class="text-center text-muted small mb-0">
                Đã nhớ mật khẩu? <a asp-page="/Auth/Login">Quay lại đăng nhập</a>
            </p>

        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}